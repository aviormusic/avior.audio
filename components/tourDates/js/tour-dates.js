var TourDatesDispatcher = new Hask.Dispatcher();
var TourDatesConstants = {
    // Defining actions here
};

TourDatesDispatcher.register(function(payload) {
    switch(payload.actionType) {
        // Handle actions here
        default: break;
    }
});

$(document).ready(function() {
    // Dispatching actions here
});
